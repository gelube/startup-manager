<Window x:Class="StartupManagerPro.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="启动管理器 Pro" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF"
        FontFamily="Microsoft YaHei UI">
    
    <Window.Resources>
        <!-- 主色调 -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#4A90D9"/>
        <SolidColorBrush x:Key="PrimaryHoverColor" Color="#3A7BC8"/>
        <SolidColorBrush x:Key="SuccessColor" Color="#52C41A"/>
        <SolidColorBrush x:Key="WarningColor" Color="#FAAD14"/>
        <SolidColorBrush x:Key="DangerColor" Color="#FF4D4F"/>
        <SolidColorBrush x:Key="TextColor" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondaryColor" Color="#666666"/>
        <SolidColorBrush x:Key="BorderColor" Color="#E8E8E8"/>
        <SolidColorBrush x:Key="BgColor" Color="#FAFAFA"/>
        
        <!-- 按钮样式 -->
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" 
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{StaticResource PrimaryHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
        </Style>
        
        <Style x:Key="WarningButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background" Value="{StaticResource WarningColor}"/>
        </Style>
        
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background" Value="{StaticResource DangerColor}"/>
        </Style>
        
        <!-- 卡片样式 -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.08"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 顶部标题 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="⚡" FontSize="28" Margin="0,0,8,0"/>
            <TextBlock Text="启动管理器 Pro" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextColor}"/>
        </StackPanel>
        
        <!-- 工具栏 -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Background="{StaticResource BgColor}" Padding="12">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddBtn" Content="➕ 添加" Style="{StaticResource ToolButton}" Click="AddNew_Click"/>
                <Button x:Name="EnableBtn" Content="✅ 启用" Style="{StaticResource SuccessButton}" Click="Enable_Click"/>
                <Button x:Name="DisableBtn" Content="⏸️ 禁用" Style="{StaticResource WarningButton}" Click="Disable_Click"/>
                <Button x:Name="DeleteBtn" Content="🗑️ 删除" Style="{StaticResource DangerButton}" Click="Delete_Click"/>
                
                <Separator Margin="16,0" Background="Transparent"/>
                
                <Border Background="White" CornerRadius="6" Padding="8,4" BorderBrush="{StaticResource BorderColor}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="🔍" FontSize="14" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBox x:Name="SearchBox" Grid.Column="1" 
                                 Background="Transparent" BorderThickness="0" 
                                 FontSize="14" Foreground="{StaticResource TextColor}"
                                 VerticalAlignment="Center"
                                 TextChanged="SearchBox_TextChanged"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Border>
        
        <!-- 主内容区 -->
        <Grid Grid.Row="2" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- 左侧分类 -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="分类" FontSize="16" FontWeight="Bold" 
                               Foreground="{StaticResource TextColor}" Margin="8,0,0,12"/>
                    
                    <ListBox x:Name="CategoryTree" Grid.Row="1" BorderThickness="0" Background="Transparent"
                             SelectionMode="Single" SelectedIndex="0"
                             SelectionChanged="CategoryTree_SelectionChanged">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd" CornerRadius="8" Padding="12,10" Background="Transparent">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Icon}" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                                    <StackPanel>
                                                        <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="Medium" Foreground="#333333"/>
                                                        <TextBlock Text="{Binding Count, StringFormat={}{0} 项}" FontSize="12" Foreground="#888888" Margin="0,2,0,0"/>
                                                    </StackPanel>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#F5F5F5"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="#E6F4FF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ContentPresenter/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
            
            <!-- 右侧列表 -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0">
                <DataGrid x:Name="StartupGrid" AutoGenerateColumns="False" 
                          CanUserAddRows="False" CanUserDeleteRows="False"
                          GridLinesVisibility="None" HeadersVisibility="Column"
                          BorderThickness="0" SelectionMode="Single"
                          Background="Transparent" AlternatingRowBackground="#FAFAFA"
                          IsReadOnly="False" MouseLeftButtonUp="StartupGrid_MouseLeftButtonUp">
                    
                    <DataGrid.ContextMenu>
                        <ContextMenu Opened="ContextMenu_Opened">
                            <MenuItem x:Name="EnableMenu" Header="启用" Click="Enable_Click" Foreground="{StaticResource TextColor}"/>
                            <MenuItem x:Name="DisableMenu" Header="禁用" Click="Disable_Click" Foreground="{StaticResource TextColor}"/>
                            <Separator x:Name="ServiceSeparator"/>
                            <MenuItem x:Name="ServiceStartMenu" Header="启动" Click="ServiceStart_Click" Foreground="{StaticResource TextColor}"/>
                            <MenuItem x:Name="ServiceStopMenu" Header="停止" Click="ServiceStop_Click" Foreground="{StaticResource TextColor}"/>
                            <Separator/>
                            <MenuItem Header="打开位置" Click="OpenLocation_Click" Foreground="{StaticResource TextColor}"/>
                            <MenuItem Header="属性" Click="Properties_Click" Foreground="{StaticResource TextColor}"/>
                            <Separator/>
                            <MenuItem Header="删除" Click="Delete_Click" Foreground="{StaticResource DangerColor}"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>
                    
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
                            <Setter Property="FontWeight" Value="Medium"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="16,12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Padding" Value="16,8"/>
                            <Setter Property="FontSize" Value="14"/>
                            <Setter Property="Foreground" Value="{StaticResource TextColor}"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#F0F5FF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                    
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Width="50">
                            <DataGridTemplateColumn.Header>
                                <CheckBox x:Name="SelectAllCheckBox" HorizontalAlignment="Center" 
                                          Checked="SelectAllCheckBox_Changed" Unchecked="SelectAllCheckBox_Changed"/>
                            </DataGridTemplateColumn.Header>
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                        <DataGridTextColumn Header="厂商" Binding="{Binding Publisher}" Width="Auto" IsReadOnly="True"/>
                        <DataGridTextColumn Header="位置" Binding="{Binding Location}" Width="Auto" IsReadOnly="True"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="120" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
        
        <!-- 底部状态栏 -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Background="{StaticResource BgColor}" Margin="0,8,0,0">
            <TextBlock x:Name="StatusTotal" Text="共 0 项" 
                       FontSize="14" Foreground="{StaticResource TextSecondaryColor}"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>